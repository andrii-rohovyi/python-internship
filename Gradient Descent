import numpy
def gradientDescent (X, y, theta, alpha, num_iters,epsilon, computeCost ):
    m = len(y)
    i = 0
    while ((i<num_iters) and (computeCost(X, y, theta)[0] >epsilon)):
        i +=1
        dJ = computeCost(X, y, theta)[1];
        theta = theta - alpha * dJ;
    return  theta

