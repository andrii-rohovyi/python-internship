import numpy as np
def computeGenerelizedCost(X, y, theta):
    m = len(y)
    d = g(X.dot(theta)) - y
    return  d.conj().T.dot(d) / (2 * m) , \
    X.conj().T.dot(g(X.dot(theta)) - y) / m 
def g(z):
    return np.exp(z) # 1 / (1 + np.exp(-z) ) #z  # model function
